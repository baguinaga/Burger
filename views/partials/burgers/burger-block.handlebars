<li class="mt-3">{{burger_name}}<br>
<button class="change-devoured btn btn-outline-info mx-2" data-id="{{id}}" data-devoured="{{devoured}}">
		{{#if devoured}}Eat It!{{else}}Make Another!{{/if}}
	</button>

	<button class="delete-burger btn btn-outline-danger mx-2" data-id="{{this.id}}">Ew...Throw it away!</button>
</li>